<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Name — Portfolio</title>
  <meta name="description" content="Personal portfolio showcasing projects, skills and contact information." />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0f1724; /* deep navy */
      --card:#0b1220;
      --muted:#94a3b8;
      --accent:#7c3aed; /* purple */
      --glass: rgba(255,255,255,0.04);
      --glass-2: rgba(255,255,255,0.02);
      font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
      color-scheme: dark;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;background:linear-gradient(180deg,var(--bg),#061124);color:#e6eef8}
    a{color:inherit;text-decoration:none}
    .container{max-width:1100px;margin:48px auto;padding:0 20px}

    header{display:flex;align-items:center;justify-content:space-between;gap:16px}
    .brand{display:flex;align-items:center;gap:12px}
    .logo{width:48px;height:48px;border-radius:12px;background:linear-gradient(135deg,#6d28d9,#4f46e5);display:grid;place-items:center;font-weight:700}
    nav{display:flex;gap:18px;align-items:center}
    nav a{color:var(--muted);font-weight:600;font-size:0.95rem}
    .cta{background:linear-gradient(90deg,var(--accent),#4f46e5);padding:10px 14px;border-radius:10px;font-weight:700}

    .hero{display:grid;grid-template-columns:1fr 360px;gap:28px;align-items:center;margin-top:28px}
    .hero-left h1{font-size:2.1rem;margin:0 0 8px}
    .hero-left p{color:var(--muted);margin:0 0 18px;line-height:1.5}
    .buttons{display:flex;gap:12px}
    button{padding:10px 14px;border-radius:10px;border:0;font-weight:700;cursor:pointer}
    .btn-outline{background:transparent;border:1px solid rgba(255,255,255,0.06)}

    .profile-card{background:linear-gradient(180deg,var(--card),#071126);padding:18px;border-radius:14px;box-shadow:0 6px 20px rgba(2,6,23,0.6)}
    .avatar{width:100%;height:320px;border-radius:10px;object-fit:cover;background:linear-gradient(180deg,#0e1724,#081026)}
    .meta{display:flex;justify-content:space-between;align-items:center;margin-top:12px}

    section{margin-top:40px}
    .grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
    .card{background:var(--glass);padding:16px;border-radius:12px;border:1px solid var(--glass-2)}
    .card h3{margin:0 0 8px}
    .projects-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:18px}
    .project{padding:14px;border-radius:10px;background:linear-gradient(180deg,rgba(255,255,255,0.02),transparent);border:1px solid rgba(255,255,255,0.03)}
    .tags{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
    .tag{padding:6px 8px;border-radius:999px;font-size:0.8rem;background:rgba(255,255,255,0.03)}

    .skills{display:flex;gap:10px;flex-wrap:wrap}
    .skill{min-width:120px}
    .bar{height:10px;background:rgba(255,255,255,0.04);border-radius:999px;overflow:hidden}
    .progress{height:100%;border-radius:999px;background:linear-gradient(90deg,var(--accent),#4f46e5)}

    footer{margin:48px 0 80px;display:flex;justify-content:space-between;align-items:center;color:var(--muted)}

    /* responsive */
    @media (max-width:920px){
      .hero{grid-template-columns:1fr;}
      .profile-card{order:-1}
      .projects-grid{grid-template-columns:1fr}
      .grid-3{grid-template-columns:1fr}
    }

    /* small helpers */
    .muted{color:var(--muted)}
    .small{font-size:0.85rem}
    input,textarea{width:100%;padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}
    label{font-weight:600;font-size:0.9rem}
    .form-row{display:grid;grid-template-columns:1fr 1fr;gap:12px}
    @media (max-width:560px){.form-row{grid-template-columns:1fr}}
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="brand">
        <div class="logo">YN</div>
        <div>
          <div style="font-weight:700">Your Name</div>
          <div class="small muted">Frontend Developer · Designer</div>
        </div>
      </div>
      <nav>
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#skills">Skills</a>
        <a href="#contact" class="cta">Get in touch</a>
      </nav>
    </header>

    <main>
      <section class="hero">
        <div class="hero-left">
          <h1>Hi, I’m <span style="background:linear-gradient(90deg,#fff 0%, #c7b3ff 100%);-webkit-background-clip:text;color:transparent;">Your Name</span> — I build delightful interfaces.</h1>
          <p class="muted">I’m a frontend developer focusing on accessible, performant, and polished web experiences. I love turning ideas into products using HTML, CSS, and JavaScript.</p>
          <div class="buttons">
            <a href="#projects"><button>See projects →</button></a>
            <a href="#contact"><button class="btn-outline">Contact me</button></a>
          </div>
          <div style="margin-top:20px;display:flex;gap:14px;align-items:center">
            <div class="card small" style="padding:10px">📍 Based in: Bangalore</div>
            <div class="card small muted" style="padding:10px">Available for freelance & full-time</div>
          </div>
        </div>

        <aside class="profile-card">
          <img class="avatar" src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?q=80&w=1200&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Profile" />
          <div class="meta">
            <div>
              <div style="font-weight:700">Your Name</div>
              <div class="muted small">Frontend Developer</div>
            </div>
            <div style="text-align:right">
              <div style="font-weight:700">5+ yrs</div>
              <div class="muted small">Experience</div>
            </div>
          </div>
        </aside>
      </section>

      <section id="about">
        <div class="grid-3">
          <div class="card">
            <h3>About me</h3>
            <p class="muted small">I craft UI with attention to detail and performance. I enjoy collaborating with product teams and designers to ship features that users love. My stack: HTML, CSS, JS, React, TypeScript.</p>
          </div>

          <div class="card">
            <h3>Contact</h3>
            <p class="small muted">Email: <a href="mailto:your.email@example.com">your.email@example.com</a></p>
            <p class="small muted">LinkedIn: <a href="#">/in/yourprofile</a></p>
            <p class="small muted">GitHub: <a href="#">@yourhandle</a></p>
          </div>

          <div class="card">
            <h3>Stats</h3>
            <p class="small muted">1200+ GitHub contributions · 20+ shipped projects · 300k monthly users reached (combined)</p>
          </div>
        </div>
      </section>

      <section id="projects">
        <h2 style="margin:0 0 16px">Selected projects</h2>
        <div class="projects-grid">
          <article class="project">
            <h3>Project One</h3>
            <p class="muted small">A modern dashboard built with React and D3 for data visualization. Includes auth, real-time updates and responsive charts.</p>
            <div class="tags">
              <div class="tag">React</div>
              <div class="tag">D3</div>
              <div class="tag">TypeScript</div>
            </div>
            <div style="margin-top:12px;display:flex;gap:10px">
              <a href="#" class="small">Live →</a>
              <a href="#" class="small">Code →</a>
            </div>
          </article>

          <article class="project">
            <h3>Project Two</h3>
            <p class="muted small">A marketing site with animations and optimized images. Focused on lighthouse performance and SEO best practices.</p>
            <div class="tags">
              <div class="tag">HTML</div>
              <div class="tag">CSS</div>
              <div class="tag">Performance</div>
            </div>
            <div style="margin-top:12px;display:flex;gap:10px">
              <a href="#" class="small">Live →</a>
              <a href="#" class="small">Code →</a>
            </div>
          </article>
        </div>
      </section>

      <section id="skills">
        <h2 style="margin:0 0 16px">Skills</h2>
        <div class="skills card" style="padding:18px">
          <div style="width:100%">
            <div class="skill">
              <div style="display:flex;justify-content:space-between"><div>HTML & CSS</div><div class="muted small">95%</div></div>
              <div class="bar" style="margin-top:8px"><div class="progress" style="width:95%"></div></div>
            </div>
            <div style="height:12px"></div>
            <div class="skill">
              <div style="display:flex;justify-content:space-between"><div>JavaScript</div><div class="muted small">90%</div></div>
              <div class="bar" style="margin-top:8px"><div class="progress" style="width:90%"></div></div>
            </div>
            <div style="height:12px"></div>
            <div class="skill">
              <div style="display:flex;justify-content:space-between"><div>React & TypeScript</div><div class="muted small">85%</div></div>
              <div class="bar" style="margin-top:8px"><div class="progress" style="width:85%"></div></div>
            </div>
          </div>
        </div>
      </section>

      <section id="contact">
        <h2 style="margin:0 0 12px">Contact</h2>
        <p class="muted small">Want to work together? Send a quick message — I’ll get back to you.</p>

        <form onsubmit="handleSubmit(event)" class="card" style="margin-top:12px">
          <div class="form-row">
            <div>
              <label for="name">Name</label>
              <input id="name" name="name" required placeholder="Your full name" />
            </div>
            <div>
              <label for="email">Email</label>
              <input id="email" name="email" type="email" required placeholder="you@example.com" />
            </div>
          </div>

          <div style="margin-top:12px">
            <label for="message">Message</label>
            <textarea id="message" name="message" rows="5" required placeholder="Tell me about your project..."></textarea>
          </div>

          <div style="margin-top:12px;display:flex;justify-content:flex-end">
            <button type="submit">Send message</button>
          </div>
        </form>
      </section>

    </main>

    <footer>
      <div>© <span id="year"></span> Your Name — Built with ❤️</div>
      <div class="muted small">Made with HTML, CSS & a little JS</div>
    </footer>
  </div>

  <script>
    // small JS helpers: year & form handling
    document.getElementById('year').textContent = new Date().getFullYear();

    function handleSubmit(e){
      e.preventDefault();
      const name = e.target.name.value.trim();
      const email = e.target.email.value.trim();
      const message = e.target.message.value.trim();
      if(!name || !email || !message){
        alert('Please fill every field.');
        return;
      }
      // In a real site you'd send this to your backend or a service like Formspree
      alert('Thanks, ' + name + '! Your message was "sent" (demo).');
      e.target.reset();
    }
  </script>
</body>
</html>
